/**
 * Symbol key that carries socket call info interpreted by this redux middleware.
 *
 * @constant {symbol}
 * @access public
 * @default
 */
const CALL_SOCKET = Symbol('Call Socket');

export default CALL_SOCKET;
