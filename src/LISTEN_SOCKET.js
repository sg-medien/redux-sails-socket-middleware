/**
 * Symbol key that carries socket listen info interpreted by this redux middleware.
 *
 * @constant {symbol}
 * @access public
 * @default
 */
const LISTEN_SOCKET = Symbol('Listen Socket');

export default LISTEN_SOCKET;
